language: php

php:
  - 5.4
  - 5.5
  - 5.6

before_script:
  - cp .env.travis .env
  - mysql -e 'create database test_timegrid;'
  - composer self-update
  - composer install --no-interaction

script:
  - vendor/bin/phpunit